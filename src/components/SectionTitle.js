import React from 'react';

const SectionTitle = ({title}) => {
  return (
    <h2 className="text-3xl font-extrabold tracking-tight text-gray-900 mb-20">
      {title}
    </h2>
  );
};

export default SectionTitle;
